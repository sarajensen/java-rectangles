<h1>Is it a square?</h1>

<h5>Your rectangle's length is $myRectangle.getLength()</h5>
<h5>Your rectangle's width is $myRectangle.getWidth()</h5>

#if ($myRectangle.isSquare())
  <h3>Your rectangle is a square!</h3>
  <h4>Surface area of a cube with those dimensions: $myCube.surfaceArea()</h4>
  <h4>Volume of a cube with those dimensions: $myCube.volume()</h4>
#else
  <h3>Your rectangle isn't a square!</h3>
#end
